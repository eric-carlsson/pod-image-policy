apiVersion: v1
kind: Service
metadata:
  name: {{ include "pod-image-admission.fullname" . }}
  labels:
    {{- include "pod-image-admission.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: https
      protocol: TCP
      name: https
  selector:
    {{- include "pod-image-admission.selectorLabels" . | nindent 4 }}
