apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pod-image-admission.fullname" . }}-test-connection"
  labels:
    {{- include "pod-image-admission.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pod-image-admission.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
