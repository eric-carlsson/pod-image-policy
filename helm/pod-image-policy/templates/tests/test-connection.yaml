apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pod-image-policy.fullname" . }}-test-connection"
  labels:
    {{- include "pod-image-policy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pod-image-policy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
